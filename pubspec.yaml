name: log_formatter
description: A web server built using the shelf package.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  args: ^2.3.0
  shelf: ^1.2.0
  shelf_router: ^1.1.2
  shelf_static: ^1.1.0

dev_dependencies:
  lints: ^1.0.1
  test: ^1.20.1
